/**
 * Letterboxing / viewport gaps use this instead of `#000` so the area reads as part of the app
 * (same as `APP_NAV_BG` in App.tsx), not a separate “black box”.
 */
export const LOADING_OVERLAY_SCRIM = '#030A17'
